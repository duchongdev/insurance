server {
    listen 80;
    server_name _;

    resolver 127.0.0.11 valid=10s ipv6=off;

    client_max_body_size 10m;

    set $backend bridge:8080;

    include /etc/nginx/snippets/bridge-locations.conf;
}
